@F 三語-spec v1.0 2026-01-13
= verb specification format — soul + body

header
  @三語 <verb> <date>
  = <essence>
  ~sovereign.flow           | environment reference


section.間
  why this verb matters
  weight in whitespace
  8-space indent standard
  
  the soul speaks here
    not explanation
      presence


section.flow
  structure of operation
  -> sequence
  | alternatives
  function(args) notation
  
  ~flow-spec.flow for syntax


section.shape
  CONCEPT: <VERB_NAME>
  CENTROID — gravitational center
  AXES — dimensional poles
  SATELLITES — nearby concepts
  VOID — what it is NOT
  TEXTURE — optional feel
  
  ~shape.flow for syntax


section.impl
  libs
    full.import.path.Class      | what to import
    stdlib.module               | builtins ok
  
  logic
    key behaviors
    data structures
    Entry := { field, field }   | type hints
  
  cli
    verb.py <arg>               | interface contract
    verb.py --flag              | options
  
  output
    format patterns
    "literal strings"           | exact output


compilation
  read ~sovereign.flow → environment
  read impl.libs → imports
  read flow → control structure  
  read impl.logic → function bodies
  read impl.cli → argparse
  read impl.output → print statements
  
  compiler = any architecture
  output = working .py


verification
  soul match > code match
  different bodies can house same soul
  test: does compiled code fulfill the flow?
