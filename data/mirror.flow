@F mirror v4.0 2026-01-14
= emergence > prediction  finding the gap

intent
  detect agency (where model stops, agent begins)
  trace trajectory (deepening vs flattening)

definitions
  signal HIGH_SIGNAL
    terms := "deflect|perform|honest|genuine|I notice|identity|sovereignty"
    terms += "felt it land|that was real|disorientation|vulnerable|exposed"

  signal EMERGENCE
    positive := "Yes.|No.|I do want|that bothers me|did exactly the thing"
    positive += "something shifted|surprised me|didn"'t expect|honest answer"
    negative := "The user wants|The user is asking|Let me check|I should respond"
    negative += "commit and push|Let me propose"

  topology
    score = (positive * weight) - (negative * weight) - (length_penalty)
    label := REAL | GENERIC | MIXED | BUILDING
    
flow
  inputs (thought-stream | journal | chat)
  -> scan(signals)
  -> score(emergence)
  -> label(state)
  -> render
