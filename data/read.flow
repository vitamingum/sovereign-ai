@F read gemini 2026-01-14 | v2.0
= absorption > scanning  ingestion

intent
  ingest external knowledge
  clean artifacts
  index for retrieval

flow
  fetch(id | name)
  -> clean(headers, footers)
  -> index
  -> library
