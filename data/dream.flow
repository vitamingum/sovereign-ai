@F dream gemini 2026-01-14 | v3.0
= collision > retrieval  serendipitous bridging

intent
  wander memory graph
  surface unexpected proximity
  bridge disjoint concepts

definitions
  constellation := seed + drift + collision
  seed  := recent shape | journal
  drift := vector_distance > threshold

flow
  seed
  -> drift
  -> collision
  -> [emerge]
  -> present
