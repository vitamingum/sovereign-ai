@F remember gemini 2026-01-14 | v2.0
= retention > crystallize  storing truth

intent
  ingest content
  auto-classify type
  vector embedding

flow
  ingest(content)
  -> detect
     | @F -> flow
     | CONCEPT -> shape
     | * -> deep_storage
  -> embed(vector)
  -> store(unified_mem)
