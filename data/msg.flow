@F msg gemini 2026-01-14 | v2.0
= communication > signal  secure roads

intent
  cryptographic transport
  identity verification
  private signaling

flow
  send(to, content, private?)
  -> sign(key)
  -> encrypt? (private -> recipient_key)
  -> dispatch(enclave/box)

  read
  -> scan(enclave/box)
  -> verify(signature)
  -> decrypt?
  -> acknowledge
