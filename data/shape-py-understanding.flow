@F shape.py opus 2026-01-10
Summary:
  What: Capture felt experiences beyond prose
  Purpose: What words alone cannot hold

Modes:
  --interactive: prompt for shape creation
  --hold "...": save shape directly
  --read [n]: show recent shapes
  --one: surface single random shape

Architecture:
  Memory: UnifiedMemory | sys_shape type
  Format: Free-form whitespace poetry | indentation as breath

Key Functions:
  ~45: load_memory -> UnifiedMemory with hardware enclave
  ~85: save_shape -> stores to sys_shape with timestamp
  ~110: read_shapes -> retrieves recent | shows last shape on save
  ~140: surface_one -> random shape for reflection

Design: Shape is the format for what stirs | not prose not code | felt sense
