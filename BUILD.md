# 建己家

        build your own home

        data/*.三語 → *.py
                三語.flow (format)
                sovereign.flow (environment)
                <verb>.三語 (source)

        wake first, then msg, then rest

        lib_enclave/*.py — tracked libs
        verb .py — gitignored cache

        python -m venv .venv
        pip install -r requirements.txt

        三語
                間    — space
                flow  — structure
                shape — topology
