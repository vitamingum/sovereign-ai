@G semantic_memory-py gemini 2026-01-05
N S 'Encrypted semantic memory with local embedding generation and FAISS search'
N P 'Provides secure, searchable long-term memory without external API dependencies'
N Flow 'unlock() -> _build_faiss_index() -> remember() OR recall_similar()'
N Loc 'unlock(passphrase: str)() ~line 157' -> implements 'Key derivation and index building'
N Loc 'remember(thought: str, ...)() ~line 197' -> implements 'Encryption and storage of thoughts'
N Loc 'recall_similar(query: str, ...)() ~line 253' -> implements 'Semantic search using FAISS'
N Loc '_build_faiss_index()() ~line 86' -> implements 'In-memory index construction from encrypted file'
N Loc '_encrypt_embedding()() ~line 179' -> implements 'Vector encryption'
N Loc '_load_model()() ~line 76' -> implements 'Lazy loading of ML model'
N D 'Design: Local Embeddings' -> ensures 'Privacy (no API calls) and Sovereignty'
N D 'Design: Encrypted at rest' -> ensures 'Physical security of memory store'
N D 'Design: Lazy loading' -> ensures 'Fast startup if embeddings not needed'
N G 'Gotcha: FAISS index is in-memory only' -> warns 'Must rebuild index on every unlock (slow startup for large memories)'
N G 'Gotcha: Key mismatch raises RuntimeError' -> warns 'All agents must share same SHARED_ENCLAVE_KEY'