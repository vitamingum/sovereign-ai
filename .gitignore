# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Backups (local only, not tracked)
backups/

# Environment
.env
.venv
env/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Bootstrap context (contains passphrase + decrypted thoughts)
.context_injection.md

# Mirror state dump (personal cognitive state)
mirror_state.md

# Runtime state files
last_bootstrap.txt
**/last_bootstrap.txt

# Regeneratable synthesis material
synthesis_material_*.txt

# Debug scripts (one-off, often contain hardcoded paths)
debug_*.py

# Test output and temp files
*.tmp
temp_*.txt
temp_*.py
test_output/
recollection_test/

# Sample/scratch scripts
sample_*.py
scratch_*.py
summarize_*.py

# Research experiment raw data (may contain PII in captured context)
# Keep the analysis, not the raw captures
research/multi_graph_experiment/*.json

# Local tool outputs
shallow_all.txt
*.log

# Backup files
*.bak
*.orig

# Jupyter checkpoints
.ipynb_checkpoints/

# ============================================================
# SECURITY: Sensitive data files - NEVER commit unencrypted
# ============================================================

# Sealed keys (DPAPI-bound)
**/key.sealed
**/*.sealed

# Encrypted key bundles
**/keys/*.enc.json

# PLAINTEXT chat index - NEVER commit (use .enc version)
data/chat_index.db
**/chat_index.db

# FAISS indexes (contain embeddings that could leak thinking patterns)
data/thinking_faiss.*
**/*.index
**/*.pkl

# Allow encrypted versions to be committed:
!data/chat_index.db.enc
!**/*.enc
!**/*.enc.json

Why.md
Why-Shape.md
